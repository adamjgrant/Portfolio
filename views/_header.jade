nav.navbar.navbar-default.navbar-fixed-top(role="navigation")
	.container
		.navbar-header
			button.navbar-toggle(type="button", data-toggle="collapse", data-target=".navbar-ex1-collapse")
				span.sr-only Toggle Navigation
				span.icon-bar
				span.icon-bar
				span.icon-bar
			a.navbar-brand.hidden-sm(href="/") Adam Kochanowicz
		.ks-afterload
			.navbar-collapse.collapse.navbar-ex1-collapse
				ul.nav.navbar-nav
					li(class="{{selectedCategory().slug == 'home' ? 'active' : ''}}")
						a(href="/") Home
					li(class="{{selectedCategory().slug == category.slug ? 'active' : ''}}", ng-repeat="category in categories")
						a.pointer(ng-click="setLocation(category.slug, 'top', true)") {{category.name}}	